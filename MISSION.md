# MISSION

## 한 줄

**Quarkus로 어렴풋이 하던 것을 Spring Boot로 제대로 설명할 수 있게 되어, 사내 이동과 이직에서 "이 사람은 안다"는 판단을 받는 것.**

---

## 왜 이걸 배우는가

독일에서 Fachinformatiker für Daten- und Prozessanalyse Ausbildung을 2026년 7월에 마쳤다. Ausbildung 기간에는 개발자로 일하며 Quarkus와 Angular를 다뤘지만, 졸업과 동시에 팀 개편으로 C# 팀에 배치됐다. 팀·상사·업무가 맞지 않아 이동을 원한다.

문제는 내 코드 지식이 **사상누각**이라는 감각이다. 동작하는 코드는 쓰지만, 왜 그렇게 되는지를 설명하지 못한다. 면접에서든 사내 이동 대화에서든 이게 드러난다.

그래서 목표는 "새 기술을 더 배우는 것"이 아니라 **이미 만져본 것을 설명 가능한 수준으로 만드는 것**이다.

---

## 최종 지향점

- 풀스택 개발자 (Java/Python 백엔드 + Angular/React 프론트엔드)
- AI 엔지니어

## 단계별 목표

| 시점 | 목표 |
|---|---|
| 3개월 (2026-11) | 사내 이동 성사. Java/Angular 역량 증명 |
| 6개월 (2027-02) | 이직 가능한 포트폴리오. 외부 지원 시작 |
| 12개월 (2027-07) | 멀티 에이전트 시스템 구현 경험 |

---

## 배움이 향하는 실물

**인보이스·영수증 관리 시스템** (`invoice-app` 저장소)

한국에서 경영학부 회계학을 전공했고, Ausbildung은 데이터·프로세스 분석이다. 이 조합은 순수 개발자에게 없는 자산이므로 프로젝트 주제로 살린다.

```
Angular → Spring Boot → FastAPI → OpenAI API
```

**모든 레슨은 이 프로젝트에서 실제로 쓸 수 있는 것이어야 한다.** 추상적인 예제는 이 미션에 부합하지 않는다.

---

## 학습 원칙

1. **"왜"가 없는 답은 도움이 안 된다.** 동작하는 코드보다 설명 가능한 코드가 목표다.
2. **완성 코드를 통째로 받지 않는다.** 힌트 → 방향 → 부분 코드 순서.
3. **에러는 먼저 읽는다.** 에러 메시지 읽는 능력 자체가 학습 목표에 포함된다.
4. **Quarkus 경험을 다리로 쓴다.** "Quarkus에서는 X였는데 Spring에서는?" 이 접근이 가장 빠르다.
5. **범위를 넓히지 않는다.** 아래 금지 목록 참조.

---

## 지금 배우지 않을 것

레슨 주제로 제안하지 말 것. 물어보면 "언제 다시 볼지"만 정하고 미룬다.

**6개월차까지:** Redis, Kafka, Kubernetes, 실제 MSA 분리, React, LangChain, RAG, GraphQL

**1년 내내:** 새 언어 추가, 자격증, "PO 에이전트가 웹앱을 생성하는 프로젝트"

> 마지막 항목 설명: 원래 하고 싶었던 아이디어지만, 좋은 웹앱의 기준이 없는 상태에서는 결과물을 평가할 수 없고, 채용에서 "직접 짠 코드는 어디 있나"라는 질문을 부른다. 대신 8개월차에 **내가 만든 앱 안에서 에이전트가 일하는** 형태로 한다.

---

## 현재 위치

| 항목 | 상태 |
|---|---|
| Java 문법 | **처음부터 다시 다짐 (2026-08-03 결정)** |
| Spring Boot | 처음 (Quarkus 감은 있음) |
| Angular | 기초 경험 있음, 복습 필요 |
| Python | 기초 학습 중 (`python-basics` 워크스페이스) |
| 현재 주차 | W01 (invoice-app) |

> 문법을 안다고 생각했지만, 실제 구현에서 막히는 지점이 계속 나와 처음부터 다시 다지기로 함. `invoice-app` 진행과 **병행**한다 — 문법 학습이 끝날 때까지 프로젝트를 멈추지 않는다.

---

## 문법 기초 큐

Spring 개념 학습(아래 "지금 배우지 않을 것" 이전 섹션들)보다 **먼저** 순서대로 다룬다. 하나가 끝나면 체크하고 다음으로.

/teach 사용 예: `/teach class와 object의 차이, constructor가 하는 일`

- [x] variables, types, type casting
- [x] control flow (if/switch/for/while) — 복습 위주, 빠르게
- [x] class와 object, constructor
- [x] access modifiers (public/private/protected) — 왜 나누는가
- [x] static vs instance
- [x] inheritance와 polymorphism
- [x] interface와 abstract class — 언제 뭘 쓰는가
- [x] collections (List, Map, Set) — 실무에서 뭘 언제 쓰는가
- [x] generics
- [x] exception handling (checked vs unchecked)
- [x] lambda와 stream

**순서대로 진행한다.** 위에서 아래로, 건너뛰지 않는다. 이게 "단단하게"의 의미다.

### 두 트랙은 분리되어 있다

| 트랙 | 언제 | 방식 | 목적 |
|---|---|---|---|
| **문법 기초 (이 큐)** | 월·수 시작 전 20분 | 큐를 순서대로, `/teach`로 제대로 | 단단한 기초 |
| **invoice-app 중 질문** | 코딩하다 막힐 때 수시 | 그 자리에서 짧게 묻고 바로 진행 | 진도를 안 끊음 |

이 둘은 서로 대체하지 않는다. invoice-app 중에 `@GetMapping`이 막혀서 짧게 물어봤다고 해서, 그게 "클래스와 객체" 항목을 배운 게 되는 건 아니다. 반대로 문법 큐를 다 돌았다고 invoice-app 진도가 저절로 나가지도 않는다.

### 진행 방식

- **월요일, 수요일**: invoice-app 코딩 시작 전 20분, 문법 큐 다음 항목을 `/teach`로 진행. 끝나면 체크.
- **토요일 집중 블록, 그 외 코딩 시간**: 막히면 그 자리에서 질문, 진도 유지. 이건 문법 큐 진행과 별개다.
- **일요일 회고 30분**: 이번 주 배운 문법 항목 중 하나를 복습(간격 반복). 새 항목이 아니라 지난 것을 다시 본다.

시간 총량: 월·수 20분씩 추가 (주당 +40분). 나머지 리듬은 그대로.

## 가용 시간

평일 2시간, 주말 3~4시간. 매일 커밋이 목표.

레슨은 **짧아야 한다.** 평일에 소화 가능한 크기가 아니면 미션에 맞지 않는다.

---

## 미션 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-08-03 | 최초 작성 |
