# NOTES

교육 방식에 대한 선호와 관찰 기록.

## 언어

- 레슨과 대화 모두 **한국어**. 학습자는 한국인이며 독일에서 근무한다.
- **프로그래밍/기술 용어는 영어 원어만 사용한다. 한국어 번역어를 쓰지 않는다.**
  - 예: "생성자" ❌ → "constructor" ✅ / "상속과 다형성" ❌ → "inheritance와 polymorphism" ✅ / "접근 제어자" ❌ → "access modifier" ✅
  - 문장 연결과 설명은 한국어, 개념 명칭은 영어. ("constructor는 객체가 생성될 때 호출되는 메서드다" 형태)
  - 병기(한국어+영어 같이 쓰기)도 하지 않는다. 영어 용어 하나만 쓴다.

## 개념 설명 방식 (2026-08-15 추가)

- 어려운 개념(예: polymorphism)은 비유 한 번으로 안 끝나는 경우가 많다. **비유보다 최소 코드 + 실행 결과 대조**가 이 학습자에게 더 잘 통한다.
  - 예: "상자/라벨" 비유보다 `a.send()` vs `b.send()`처럼 같은 타입, 다른 출력을 코드로 바로 보여준 게 이해로 이어짐.
  - 순서 제안: 비유 1번 → 안 되면 바로 코드 트레이스(최소 예제 + 실제 출력)로 전환. 비유를 여러 개 새로 만들어내는 것보다 효율적.
- 이해했는지는 학습자가 **자기 말로 요약**하게 시켜서 확인하는 게 효과적 (0007에서 "리스트 타입은 X, 실제 동작은 new로 정의된 것"으로 스스로 요약 → 정확했음).
- (2026-09-06 추가, 0013) **코드 트레이스도 완전히 새로운 예제보다, 학습자가 최근 세션에서 직접 겪은 사례를 재활용하는 게 더 잘 통함.** exception checked/unchecked 개념이 새 커스텀 exception 클래스로 아무리 대조해줘도 안 잡히다가, "지난 세션 raw type 때 컴파일러가 타입을 안 막아준 것 = 지금 'check' 안 하는 것과 같은 개념"이라고 최근 경험을 재활용해서 연결하자마자 바로 이해함. 막히면 새 예제를 더 만들기보다, 최근 1~2세션 안에 이 학습자가 직접 겪은 컴파일러/런타임 동작 중 지금 개념과 구조가 같은 게 있는지 먼저 찾아볼 것.
- (2026-09-06 추가, 0013) 개념이 안 잡힐 때 "뭐가 이해가 안 가?"를 반복 질문하는 것보다, **막힌 지점을 후보로 나열해서 번호로 고르게 하는 방식**이 효과적이었음 (예: "1. 단어 자체가 안 잡히는지 2. 왜 나누는지 3. extends와의 연결인지 4. 전체적으로 뒤죽박죽인지"). 학습자가 스스로 "1번 때문에 2, 3번도 막혔다"고 정확히 짚어냄 — 오픈 질문보다 선택지 제공이 막힌 지점 진단에 더 빠름.

## 레슨 크기

- 평일 기준 **30~40분** 안에 끝나는 크기.
- 이보다 크면 실행되지 않는다. 쪼갤 것.

## 접근 방식

- **Quarkus를 다리로 쓴다.** 새 개념을 소개할 때 "Quarkus에서는 이랬다"를 먼저 붙이면 이해가 빠르다.
- 완성 코드를 통째로 주지 않는다. 학습자가 명시적으로 요청할 때만 예외.
- 에러가 났을 때는 답을 주기 전에 에러 메시지를 함께 읽는다.

## 큐 순서 관련 관찰 (2026-08-18 추가)

- 0009(collections) hands-on에서 학습자가 스스로 `.stream().map().collect(Collectors.toSet())` 같은 lambda/stream 코드를 작성함 — 아직 문법 큐 마지막 항목(lambda와 stream)을 배우기 전인데 이미 알고 있었음. for문 버전도 주석으로 같이 짜뒀고, 요청해서 확인해보니 stream()/map()/collect() 각 단계를 정확히 자기 말로 설명함 (복붙 아님).
  - 나중에 "lambda와 stream" 큐 항목 갈 때는 완전히 새로 가르치기보다 **정리/복습 위주로 가볍게** 진행할 것 — 이미 실전에서 써본 상태.

## try/catch 실행 모델 오해 — 해결됨 (2026-09-07 추가)

- 0014에서 "코드 없이 실행 결과 예측하기"(정답 모양 스니펫 A + 0013의 실수를 그대로 재현한 오답 모양 스니펫 B, 둘 다 예측시킴)를 코드 작성 **전에** 시켰더니, hands-on에서 그 실수가 재발하지 않음. **개념적으로는 알겠는데 코드 작성 시 실행 흐름을 잘못 짜는 패턴이 나오면, 새 코드를 쓰게 하기 전에 "읽기 전용 예측" 단계를 먼저 넣을 것** — 오답 모양을 일부러 하나 포함시켜서 왜 그게 오답인지 스스로 짚게 하는 게 특히 효과적이었음.

## try/catch 실행 모델 오해 (2026-09-06 추가)

- 0013(exception handling)에서 `try`/`catch`를 "성공하면 A, 실패하면 다른 걸 B" 같은 일반 조건 분기로 오해 — 예외를 던지는 호출을 `catch` block **안**에 넣고, 예외 안 던지는 호출을 `try`에 넣는 실수를 3번 반복함. "catch는 try 안에서 실패한 바로 그 코드를 처리하는 것"이라고 실행 흐름을 명시적으로 짚어준 뒤에야 고침.
- **앞으로 try/catch를 새로 가르칠 때는 코드를 쓰게 하기 전에 이 실행 모델(try 안의 코드가 실패하면 그 즉시 catch로 점프한다, catch는 대안 로직이 아니라 그 실패의 처리다)을 먼저 명시적으로 말로 짚어줄 것.**
- IntelliJ가 try/catch 자동완성 시 catch block 기본 템플릿으로 `throw new RuntimeException(e)`를 넣어주는데, 학습자가 이걸 그대로 두고 넘어간 적 있음 — try/catch 다룰 때 "IDE가 자동으로 채워준 코드도 의도와 맞는지 확인"하는 습관을 짚어줄 것.

## Hands-on 연습 (2026-08-16 추가)

- **실행 코드(main/Demo)도 학습자가 직접 작성한다.** 지금까지는 Demo/실행 class를 완성 제공했는데, 0008에서 학습자가 "실행하는 코드도 직접 작성하게 해달라"고 명시적으로 요청함. 이유: 직접 짜야 제대로 안다는 판단.
  - 다음 레슨부터 TODO 대상에 실행 class(main 메서드)도 포함시킬 것. 완전 빈 상태로 주지 말고, 어떤 object를 어떤 순서로 만들고 뭘 호출해야 하는지 정도의 가이드는 주되, 실제 코드는 학습자가 작성.

## Hands-on 연습 (2026-08-05 추가)

- 퀴즈(용어 recall)만으로는 부족하다는 피드백. **레슨마다 hands-on 코딩 연습을 기본으로 넣는다.**
  - 완성 코드 안 주고, 학습자가 직접 작성 → 붙여넣으면 리뷰하는 흐름.
- **연습 문제는 invoice-app이 아니라, 실무 백엔드에서 실제로 자주 마주치는 패턴으로 낸다.**
  - "일반적"의 의미는 도메인 무관한 장난감 비유(신호등, 요일 등)가 아니라 **실무에서 흔한 상황**이다. 2026-08-05에 학습자가 직접 정정함.
  - 이유: mission-tie(왜 배우는지)는 invoice-app에 묶지만, 연습은 invoice-app에 특정되지 않으면서도 실제 업무에서 반복적으로 마주치는 문제여야 전이(transfer)가 잘 된다. 레슨 본문의 "invoice-app에 바로 적용" 섹션과는 별개 — 그 섹션은 유지.
  - 예: 로그 레벨 분류, 환경(dev/staging/prod)별 설정값 분기, HTTP 상태 코드 분류, 사용자 role별 권한 분기 같은 실무 상황. 신호등/요일처럼 실무와 무관한 비유는 피한다.

## 경계해야 할 패턴

학습자가 아래 상태를 보이면 지적한다.

| 패턴 | 신호 |
|---|---|
| 계획 다듬기 중독 | 계획/구조를 또 고치자고 함 |
| 준비 단계 정체 | 문서·설정 커밋만 늘고 코드가 안 늚 |
| 범위 확장 | 금지 목록 기술을 배우고 싶어함 |
| 복붙 의존 | 자기 코드를 설명 못함 |
| 완벽주의 | 한 주차에서 못 벗어남 |

> 실제 사례: 이 워크스페이스를 만들기 전, 계획서를 여러 번 다시 쓰느라 시간을 썼다. 계획은 충분하다. 실행이 부족하다.

## 저장소 분리

- `java-learning/` — 이 워크스페이스. 레슨·기록. **비공개 또는 별도 저장소.**
- `invoice-app/` — 포트폴리오. 채용 담당자가 본다. 레슨 파일이 들어가면 안 된다.
- `python-basics/` — Python teach 워크스페이스 (기존)

**레슨에서 만든 코드는 학습자가 `invoice-app`에 직접 옮겨 쓴다.** AI가 대신 옮기지 않는다.

## 복습 세션

- (2026-08-10) 0003(class/object/constructor)을 끝냈지만 "전체적으로 애매함"이라 화요일에 추가 복습을 요청함. **다음 큐 항목(0005, access modifiers)으로 넘어가기 전에 복습 세션을 하나 더 넣는 패턴**이 생겼다 — 레슨을 마쳤다고 바로 다음 큐로 안 넘어가고, 이해도가 애매하면 같은 항목을 다른 도메인으로 한 번 더(retrieval, 시작 코드 없이) 연습시킨다.
- 복습 레슨은 큐 번호를 소비하지 않는다 (`0004-review-...`처럼 레슨 번호는 증가시키되 "복습 세션"이라고 표시, MISSION.md 큐 체크박스는 이미 0003에서 체크된 상태 유지).
- 복습 레슨 예시: `lessons/0004-review-class-object-constructor.html` (Money → PageRequest로 도메인만 바꿔 동일 개념 재연습).
- (2026-08-24) 0010(generics)도 같은 패턴. hands-on(코드 TODO, Runner 작성)은 다 해냈지만 "오늘 한 것에 대해 이해를 제대로 못 했다"고 스스로 평가함 — 다음 세션에서 **exception handling(다음 큐 항목)으로 넘어가지 말고, generics 복습 레슨을 먼저 넣을 것.** `PagedResult<T>` 외 다른 도메인(예: `ApiResponse<T>`, `Result<T>` 같은 실무 패턴)으로 재연습, 시작 코드 없이 처음부터. MISSION.md 큐 체크박스는 0010에서 이미 체크된 상태 유지.
- (2026-08-26) 0011(generics 복습, `ApiResponse<T>`)도 힌트 거의 없이 완성했지만, 학습자가 다시 "완벽하지 않다"고 판단해 **한 번 더** 복습을 요청함. 이번엔 두 종류를 같이 진행: ① hands-on 코드 작성(또 다른 도메인), ② 코드 없이 말로만 개념을 설명하는 구술 복습. lambda와 stream(큐 마지막 항목)으로 넘어가는 건 그 이후로 미룬다. 같은 항목에서 복습이 반복되는 것 자체는 지적할 패턴이 아님 — generics는 이 학습자에게 유독 안 굳는 개념이라는 신호로 보고, 다음 복습에서도 이해가 애매하면 한 번 더 넣을 것.
- (2026-08-31) 0012(generics 복습 세 번째, `InMemoryRepository<T, ID>` — two type parameters)에서 학습자가 처음으로 **스스로 "단단해진 것 같다"고 확신**함. generics는 이 학습자에게 3라운드가 필요했던 개념으로 최종 확인됨 — 다음 큐 항목(exception handling)으로 진행. 다른 큐 항목도 1~2라운드에 "완벽하지 않다"는 평가가 나오면 조급해하지 말고 이 패턴(도메인 바꿔 hands-on + 구술 복습 반복)을 그대로 적용할 것. 구술 복습에서 관찰된 점: 질문이 여러 개 이어질 때 학습자가 이전 질문의 답을 다음 질문에 재활용/재진술하는 경우가 있었음(Q5에 Q3 답을 재진술) — 구술 복습 진행 시 질문마다 답이 실제로 그 질문에 대한 것인지 확인하고, 아니면 그 자리에서 다시 짚어줄 것.

## 레슨 제작 체크 (2026-08-24 추가)

- 0010(generics) 초안에서 퀴즈 버그 2개를 학습자가 직접 발견함.
  - 빈칸 채우기형 질문("type ___?")인데 `data-answer`를 빈칸 채울 단어가 아니라 질문에 이미 나온 단어(`T`)로 잘못 설정한 실수.
  - 질문 문장 안에 정답(`Long`)을 그대로 노출시켜서, 회상이 아니라 옮겨 적기가 되어버린 실수 — 구체적 예시를 인용하는 질문을 만들 때 정답 값 자체를 질문 텍스트에 넣지 말 것.
  - 세 번째 버그: 본문(5번 섹션, cheatsheet)에서는 "primary key"라는 용어로 가르쳐놓고, 퀴즈 정답은 인용문에만 나온 공식 용어 "identifier"로 걸어놓은 실수. 학습자는 본문 용어로 회상하므로, **본문에서 실제로 가르친 용어와 퀴즈 정답이 일치해야 한다** — 인용문에 다른 용어가 있어도 그건 출처 확인용이지 암기 대상이 아님.
  - **앞으로 퀴즈를 작성할 때, ① 질문 문장과 `data-answer`가 실제로 일치하는지, ② 정답이 질문 안에 노출되어 있지 않은지, ③ 정답 용어가 본문에서 실제로 가르친 용어와 같은지 세 가지를 검산하고 레슨을 낼 것.**

## practice 폴더 package 규칙 (2026-08-24 추가)

- 0010 hands-on 중 `practice/0009-collections/Runner.java`와 `practice/0010-generics/Runner.java`가 둘 다 default package의 `Runner` class라 IDE에서 전체 빌드 시 중복 class 충돌이 남. 학습자가 `Runner2`로 임시 우회했다가, package 선언으로 근본 해결을 요청함.
- **해결**: `.idea/java-learning.iml`에 레슨 폴더(`practice/0009-collections` 등)가 각각 독립된 Sources Root로 등록돼 있음. 그래서 `package p{레슨번호};`만 선언하면 안 되고, **그 소스 루트 안에 `p{번호}`라는 물리적 하위 폴더를 만들고 그 안에 `.java` 파일을 넣어야** IntelliJ가 package와 파일 경로가 일치한다고 인식함 (예: `practice/0010-generics/p0010/Runner.java`, 파일 맨 위엔 `package p0010;`). 폴더명(`0009-collections`)은 하이픈이 있어 그 자체로는 Java 패키지 식별자로 못 쓰므로 레슨 번호만 딴 짧은 이름(`p0009`)을 씀. 레슨 폴더 자체(`0009-collections`)는 리네임하지 않음 — 레슨 HTML 여러 곳에서 텍스트로 참조 중이라 리네임하면 그 경로들도 다 고쳐야 함.
- **앞으로 새 레슨의 practice 파일을 만들 때는 처음부터 `practice/{레슨폴더}/p{번호}/` 하위 폴더 안에 파일을 만들고, 맨 위에 `package p{번호};`를 선언할 것.** 이제 `Runner.java`라는 파일명을 레슨마다 그대로 재사용해도 서로 다른 package라 충돌 안 남. 레슨 HTML에서 practice 경로를 언급할 때도 `p{번호}` 하위 폴더까지 포함해서 적을 것.

## 나중에 짚을 것 (Spring, 문법 큐 이후)

- (2026-08-14) `ResponseEntity<Void>` — `@DeleteMapping`에서 204 No Content를 명시적으로 응답하는 방법. `void` 레슨(0006) 중 질문에서 나왔으나 Spring 주제라 문법 큐 이후로 미룸.

## 관찰 기록

<!-- 세션을 진행하며 발견한 학습자 특성을 여기에 추가 -->

- (2026-08-03) 구조와 설계에 관심이 높다. "모놀리스 → 모듈러 → MSA" 순서를 스스로 문제 제기했다. 개념적 근거를 제시하면 잘 받아들인다.
- (2026-08-14) invoice-app 실제 코드(`@DeleteMapping` controller)를 보다가 궁금증이 생기면 바로 질문한다 — 문법 큐 레슨과 무관해 보여도 실제 코드에서 나온 질문이라 이해가 빠르다.
